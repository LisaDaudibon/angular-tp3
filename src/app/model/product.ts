export class Product {
    constructor( public title: string, public description: string, public photo : string, public price: number) {
        this.title = title;
        this.description = description;
        this.photo = photo;
        this.price = price;
    }

}
